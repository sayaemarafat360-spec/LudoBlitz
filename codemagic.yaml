workflows:
  debug:
    name: Debug Build
    max_build_duration: 15
    environment:
      java: 17
    scripts:
      - chmod +x gradlew
      - ./gradlew assembleDebug
    artifacts:
      - app/build/outputs/**/*.apk
